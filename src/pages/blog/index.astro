---
import {latestFirst, published} from './templates/blog.ts'
const posts = latestFirst(published())
---

<h1>Blog posts</h1>
<ul>
  {posts.map(post => <li><a href={post.url}>{new Date(post.frontmatter.pubDate!).toISOString().split('T')[0]}: {post.frontmatter.title}</a></li>)}
</ul>
